allow init op1_file:dir mounton;
allow init op2_file:dir mounton;
allow init proc_touchpanel:file { open read setattr };
allow init sysfs_audio_gain:file { open read setattr };
allow init sysfs_inpocket:file {  open read setattr };
allow init sysfs_dm:file { setattr };
allow init socket_device:sock_file { unlink setattr create };
allow init debugfs_tracing_debug:dir mounton;
